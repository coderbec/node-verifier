const lib = require('./lib');

lib.Configuration.hmacAuthUserName = "0HlknKPfNXXC3MXOcMbr";
lib.Configuration.hmacAuthPassword = "CraZaWRQSqRS0nKpbDeKXHX0XOZfhK";

var controller = lib.MessagesController;

let body = new lib.SendMessagesRequest();

body.messages = [];

body.messages[0] = new lib.Message();

body.messages[0].content = 'Hello world!';
body.messages[0].destinationNumber = '+61451325027';

controller.sendMessages(body, function(error, response, context) {
  if (error) {
    console.log(error);
  } else {
    console.log(response);
  }
});